package net.toryx.witcherslegacy.config;

import eu.midnightdust.lib.config.MidnightConfig;

public class WitchersLegacyConfig extends MidnightConfig {
    @Comment(category = "settings", centered = true) public static Comment comment_1;
    /*@Entry(category = "settings") public static boolean showTooltips = true;*/ // You can uncomment this if you want, and use it, but not required, just an example.
}
